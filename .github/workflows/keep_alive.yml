name: Keep Railway Alive
on:
  schedule:
    - cron: '*/20 * * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Railway Backend
        run: curl -f https://web-production-b37b2.up.railway.app/api/v1/health || true
